{
  "name": "@bot/wrapper",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "cd ../agent && boost_env/bin/python -m uvicorn main:app --reload --port 8000",
    "start": "cd ../agent && boost_env/bin/python -m uvicorn main:app --port 8000"
  }
}
